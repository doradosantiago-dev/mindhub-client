// MODELO: Follow - FollowRequest (interface)

/**
 * Interfaz para seguir/dejar de seguir a un usuario.
 * Alineada EXACTAMENTE con FollowRequest DTO del backend.
 */
export interface FollowRequest {
  userId: number;
}
